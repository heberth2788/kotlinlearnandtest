package com.bbs.designpatterns.creational.builder

/**
 * The builder pattern allows to create a complex objects step by step
 * using the same construction code. Without using complex inheritance
 * or a constructor with a lot of parameters.
 *
 * Utility : use it when you have a constructor with a lot of
 *      optional parameters.
 */

class BuilderPattern {

    fun useBuildPattern() {
        val myDirector = MyDirector()


    }
}